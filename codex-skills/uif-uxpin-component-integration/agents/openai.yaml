interface:
  display_name: "UIF UXPin Integration"
  short_description: "Build editable UIF UXPin components"
  default_prompt: "Use $uif-uxpin-component-integration to adapt this Hexa UI component for UXPin Merge with editable layers and safe defaults."

policy:
  allow_implicit_invocation: true
